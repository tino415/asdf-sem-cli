#!/bin/sh

OS=$(uname -s)
ARCH=$(uname -m)

curl --fail -L "https://github.com/semaphoreci/cli/releases/download/v${ASDF_INSTALL_VERSION}/sem_${OS}_${ARCH}.tar.gz" -o ${ASDF_DOWNLOAD_PATH}/sem.tar.gz

